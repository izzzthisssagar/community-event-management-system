@inherits LayoutComponentBase

@* A minimal full-screen layout used only by the login page, so the login screen is a clean,    *@
@* standalone experience with a colourful gradient background instead of the app sidebar.        *@

<div class="login-shell">
    @Body
</div>
